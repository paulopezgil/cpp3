%%

// don't replace strings in comments
"//".*$         out() << matched();

\"[^\"]*\"      {
                    ++d_num;
                    out() << "grabbed(" << d_num << ", \"" << filename() << "\")";
                };
